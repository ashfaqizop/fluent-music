name: core
description: Shared Result/error types, logging, and constants for Fluent Music.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  logging: any
  meta: any

dev_dependencies:
  test: any

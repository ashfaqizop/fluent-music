name: remote_config
description: Signed remote-config fetch/verify/apply for extraction self-healing (Masterdoc §6.5).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  core:
    path: ../core
  crypto: any
  cryptography: any
  dio: any
  path: any

dev_dependencies:
  test: any
